export const COMMAND_PREVIEW_READY_STATUS = "preview-ready" as const;
export const COMMAND_PREVIEW_BLOCKED_STATUS = "blocked" as const;
export const COMMAND_PREVIEW_INVALID_STATUS = "invalid" as const;
export const COMMAND_PREVIEW_UNSUPPORTED_STATUS = "unsupported" as const;

export const COMMAND_BUS_EXECUTION_BLOCKED_REASON = "Command execution is blocked in Phase 6B. Only dry-run preview planning is available.";
